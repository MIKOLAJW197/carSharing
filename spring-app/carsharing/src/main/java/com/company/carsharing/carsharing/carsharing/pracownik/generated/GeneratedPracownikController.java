package com.company.carsharing.carsharing.carsharing.pracownik.generated;

import com.company.carsharing.carsharing.carsharing.pracownik.Pracownik;
import com.company.carsharing.carsharing.carsharing.pracownik.PracownikManager;
import com.company.carsharing.carsharing.carsharing.pracownik.generated.GeneratedPracownik.Identifier;
import com.speedment.common.annotation.GeneratedCode;
import com.speedment.common.json.Json;
import com.speedment.enterprise.plugins.json.JsonCollectors;
import com.speedment.enterprise.plugins.json.JsonComponent;
import com.speedment.enterprise.plugins.json.JsonEncoder;
import com.speedment.enterprise.plugins.spring.runtime.AbstractFilter;
import com.speedment.enterprise.plugins.spring.runtime.AbstractSort;
import com.speedment.enterprise.plugins.spring.runtime.ControllerUtil;
import com.speedment.runtime.field.Field;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.CrossOrigin;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RequestParam;

import java.util.Comparator;
import java.util.EnumSet;
import java.util.List;
import java.util.Optional;
import java.util.Set;
import java.util.function.Predicate;
import java.util.stream.Stream;
import javax.annotation.PostConstruct;

import static java.util.stream.Collectors.toList;

/**
 * The default REST controller logic for Pracownik entities.
 * <p>
 * This file has been automatically generated by Speedment. Any changes made to
 * it will be overwritten.
 * 
 * @author Speedment
 */
@GeneratedCode("Speedment")
@CrossOrigin(origins = "*", maxAge = 3600)
public abstract class GeneratedPracownikController {
    
    protected @Autowired JsonComponent jsonComponent;
    protected @Autowired PracownikManager manager;
    protected JsonEncoder<Pracownik> encoder;
    
    @PostConstruct
    void createPracownikEncoder() {
        encoder = jsonComponent.<Pracownik>emptyEncoder()
            .put("pesel", Pracownik.PESEL)
            .put("imie", Pracownik.IMIE)
            .put("nazwisko", Pracownik.NAZWISKO)
            .put("bazaLokalizacja", Pracownik.BAZA_LOKALIZACJA)
            .build();
    }
    
    @GetMapping(path = "/pracownik", produces = "application/json")
    public String get(
            @RequestParam(name = "filter", defaultValue = "[]") String filters,
            @RequestParam(name = "sort", defaultValue = "[]") String sorters,
            @RequestParam(value = "start", defaultValue = "0") long start,
            @RequestParam(value = "limit", defaultValue = "25") long limit) {
        
        return getHelper(
            ControllerUtil.parseFilters(filters, PracownikFilter::new).collect(toList()),
            ControllerUtil.parseSorts(sorters, PracownikSort::new).collect(toList()),
            start,
            limit
        );
    }
    
    protected final Set<Identifier> parseColumns(String jsonColumnList) {
        try {
            @SuppressWarnings("unchecked")
            final List<String> parsed = (List<String>) Json.fromJson(jsonColumnList);
            final Set<GeneratedPracownik.Identifier> result = EnumSet.noneOf(GeneratedPracownik.Identifier.class);
            parsed.stream().map(this::parseColumn).forEach(result::add);
            return result;
        } catch (final ClassCastException ex) {
            throw new IllegalArgumentException("Error in parsed JSON.");
        }
    }
    
    protected final Identifier parseColumn(String jsonColumn) {
        switch (jsonColumn) {
            case "pesel":           return GeneratedPracownik.Identifier.PESEL;
            case "imie":            return GeneratedPracownik.Identifier.IMIE;
            case "nazwisko":        return GeneratedPracownik.Identifier.NAZWISKO;
            case "bazaLokalizacja": return GeneratedPracownik.Identifier.BAZA_LOKALIZACJA;
            default: throw new IllegalArgumentException(
                "Unknown column '" + jsonColumn + "'."
            );
        }
    }
    
    protected final Field<Pracownik> fieldOf(Identifier columnId) {
        switch (columnId) {
            case PESEL:            return Pracownik.PESEL;
            case IMIE:             return Pracownik.IMIE;
            case NAZWISKO:         return Pracownik.NAZWISKO;
            case BAZA_LOKALIZACJA: return Pracownik.BAZA_LOKALIZACJA;
            default: throw new IllegalArgumentException(
                "Unknown column '" + columnId + "'."
            );
        }
    }
    
    protected String getHelper(
            List<Predicate<Pracownik>> predicates,
            List<Comparator<Pracownik>> sorters,
            long start,
            long limit) {
        Stream<Pracownik> stream = manager.stream();
        
        for (final Predicate<Pracownik> predicate : predicates) {
            stream = stream.filter(predicate);
        }
        
        if (!sorters.isEmpty()) {
            final Optional<Comparator<Pracownik>> comparator = sorters.stream()
                .reduce(Comparator::thenComparing);
            
            stream = stream.sorted(comparator.get());
        }
        
        return stream
            .skip(start)
            .limit(limit)
            .collect(JsonCollectors.toList(encoder));
    }
    
    /**
     * How to filter the results from the controller. This class is designed to
     * be deserialized automatically from JSON.
     */
    public final static class PracownikFilter extends AbstractFilter<Pracownik> {
        
        public PracownikFilter(
                String operator,
                String property,
                String value) {
            super(operator, property, value);
        }
        
        @Override
        public Predicate<Pracownik> toPredicate() {
            switch (property()) {
                case "pesel" : {
                    final int v = Integer.parseInt(value());
                    switch (operator()) {
                        case "eq"   : return Pracownik.PESEL.equal(v);
                        case "ne"   : return Pracownik.PESEL.notEqual(v);
                        case "lt"   : return Pracownik.PESEL.lessThan(v);
                        case "le"   : return Pracownik.PESEL.lessOrEqual(v);
                        case "gt"   : return Pracownik.PESEL.greaterThan(v);
                        case "ge"   : return Pracownik.PESEL.greaterOrEqual(v);
                        case "like" : // Fallthrough
                        default : throw new IllegalArgumentException(
                            "'" + operator() + "' is not a valid operator for " +
                            "Pracownik.pesel."
                        );
                    }
                }
                case "imie" : {
                    final String v = value();
                    switch (operator()) {
                        case "eq"   : return Pracownik.IMIE.equal(v);
                        case "ne"   : return Pracownik.IMIE.notEqual(v);
                        case "lt"   : return Pracownik.IMIE.lessThan(v);
                        case "le"   : return Pracownik.IMIE.lessOrEqual(v);
                        case "gt"   : return Pracownik.IMIE.greaterThan(v);
                        case "ge"   : return Pracownik.IMIE.greaterOrEqual(v);
                        case "like" : return Pracownik.IMIE.contains(v);
                        default : throw new IllegalArgumentException(
                            "'" + operator() + "' is not a valid operator for " +
                            "Pracownik.imie."
                        );
                    }
                }
                case "nazwisko" : {
                    final String v = value();
                    switch (operator()) {
                        case "eq"   : return Pracownik.NAZWISKO.equal(v);
                        case "ne"   : return Pracownik.NAZWISKO.notEqual(v);
                        case "lt"   : return Pracownik.NAZWISKO.lessThan(v);
                        case "le"   : return Pracownik.NAZWISKO.lessOrEqual(v);
                        case "gt"   : return Pracownik.NAZWISKO.greaterThan(v);
                        case "ge"   : return Pracownik.NAZWISKO.greaterOrEqual(v);
                        case "like" : return Pracownik.NAZWISKO.contains(v);
                        default : throw new IllegalArgumentException(
                            "'" + operator() + "' is not a valid operator for " +
                            "Pracownik.nazwisko."
                        );
                    }
                }
                case "bazaLokalizacja" : {
                    final String v = value();
                    switch (operator()) {
                        case "eq"   : return Pracownik.BAZA_LOKALIZACJA.equal(v);
                        case "ne"   : return Pracownik.BAZA_LOKALIZACJA.notEqual(v);
                        case "lt"   : return Pracownik.BAZA_LOKALIZACJA.lessThan(v);
                        case "le"   : return Pracownik.BAZA_LOKALIZACJA.lessOrEqual(v);
                        case "gt"   : return Pracownik.BAZA_LOKALIZACJA.greaterThan(v);
                        case "ge"   : return Pracownik.BAZA_LOKALIZACJA.greaterOrEqual(v);
                        case "like" : return Pracownik.BAZA_LOKALIZACJA.contains(v);
                        default : throw new IllegalArgumentException(
                            "'" + operator() + "' is not a valid operator for " +
                            "Pracownik.bazaLokalizacja."
                        );
                    }
                }
                default : throw new IllegalArgumentException(
                    "'" + property() + "' is not a valid Pracownik property."
                );
            }
        }
    }
    
    /**
     * How to sort the results from the controller. This class is designed to be
     * deserialized automatically from JSON.
     */
    public final static class PracownikSort extends AbstractSort<Pracownik> {
        
        public PracownikSort(String property, String direction) {
            super(property, direction);
        }
        
        @Override
        public Comparator<Pracownik> toComparator() {
            final Comparator<Pracownik> comparator;
            switch (property()) {
                case "pesel"           : comparator = Pracownik.PESEL.comparator();            break;
                case "imie"            : comparator = Pracownik.IMIE.comparator();             break;
                case "nazwisko"        : comparator = Pracownik.NAZWISKO.comparator();         break;
                case "bazaLokalizacja" : comparator = Pracownik.BAZA_LOKALIZACJA.comparator(); break;
                default : throw new IllegalArgumentException(
                    "'" + property() + "' is not a valid Pracownik property."
                );
            }
            
            switch (direction()) {
                case "ASC"  : return comparator;
                case "DESC" : return comparator.reversed();
                default : throw new IllegalArgumentException(
                    "'" + direction() + "' is not a valid sort direction. " +
                    "Use either 'ASC' or 'DESC', or leave out."
                );
            }
        }
    }
}