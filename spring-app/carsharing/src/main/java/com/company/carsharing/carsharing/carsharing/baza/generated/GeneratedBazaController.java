package com.company.carsharing.carsharing.carsharing.baza.generated;

import com.company.carsharing.carsharing.carsharing.baza.Baza;
import com.company.carsharing.carsharing.carsharing.baza.BazaManager;
import com.company.carsharing.carsharing.carsharing.baza.generated.GeneratedBaza.Identifier;
import com.speedment.common.annotation.GeneratedCode;
import com.speedment.common.json.Json;
import com.speedment.enterprise.plugins.json.JsonCollectors;
import com.speedment.enterprise.plugins.json.JsonComponent;
import com.speedment.enterprise.plugins.json.JsonEncoder;
import com.speedment.enterprise.plugins.spring.runtime.AbstractFilter;
import com.speedment.enterprise.plugins.spring.runtime.AbstractSort;
import com.speedment.enterprise.plugins.spring.runtime.ControllerUtil;
import com.speedment.runtime.field.Field;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.CrossOrigin;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RequestParam;

import java.util.Comparator;
import java.util.EnumSet;
import java.util.List;
import java.util.Optional;
import java.util.Set;
import java.util.function.Predicate;
import java.util.stream.Stream;
import javax.annotation.PostConstruct;

import static java.util.stream.Collectors.toList;

/**
 * The default REST controller logic for Baza entities.
 * <p>
 * This file has been automatically generated by Speedment. Any changes made to
 * it will be overwritten.
 * 
 * @author Speedment
 */
@GeneratedCode("Speedment")
@CrossOrigin(origins = "*", maxAge = 3600)
public abstract class GeneratedBazaController {
    
    protected @Autowired JsonComponent jsonComponent;
    protected @Autowired BazaManager manager;
    protected JsonEncoder<Baza> encoder;
    
    @PostConstruct
    void createBazaEncoder() {
        encoder = jsonComponent.<Baza>emptyEncoder()
            .put("lokalizacja", Baza.LOKALIZACJA)
            .put("liczbaMiejsc", Baza.LICZBA_MIEJSC)
            .build();
    }
    
    @GetMapping(path = "/baza", produces = "application/json")
    public String get(
            @RequestParam(name = "filter", defaultValue = "[]") String filters,
            @RequestParam(name = "sort", defaultValue = "[]") String sorters,
            @RequestParam(value = "start", defaultValue = "0") long start,
            @RequestParam(value = "limit", defaultValue = "25") long limit) {
        
        return getHelper(
            ControllerUtil.parseFilters(filters, BazaFilter::new).collect(toList()),
            ControllerUtil.parseSorts(sorters, BazaSort::new).collect(toList()),
            start,
            limit
        );
    }
    
    protected final Set<Identifier> parseColumns(String jsonColumnList) {
        try {
            @SuppressWarnings("unchecked")
            final List<String> parsed = (List<String>) Json.fromJson(jsonColumnList);
            final Set<GeneratedBaza.Identifier> result = EnumSet.noneOf(GeneratedBaza.Identifier.class);
            parsed.stream().map(this::parseColumn).forEach(result::add);
            return result;
        } catch (final ClassCastException ex) {
            throw new IllegalArgumentException("Error in parsed JSON.");
        }
    }
    
    protected final Identifier parseColumn(String jsonColumn) {
        switch (jsonColumn) {
            case "lokalizacja":  return GeneratedBaza.Identifier.LOKALIZACJA;
            case "liczbaMiejsc": return GeneratedBaza.Identifier.LICZBA_MIEJSC;
            default: throw new IllegalArgumentException(
                "Unknown column '" + jsonColumn + "'."
            );
        }
    }
    
    protected final Field<Baza> fieldOf(Identifier columnId) {
        switch (columnId) {
            case LOKALIZACJA:   return Baza.LOKALIZACJA;
            case LICZBA_MIEJSC: return Baza.LICZBA_MIEJSC;
            default: throw new IllegalArgumentException(
                "Unknown column '" + columnId + "'."
            );
        }
    }
    
    protected String getHelper(
            List<Predicate<Baza>> predicates,
            List<Comparator<Baza>> sorters,
            long start,
            long limit) {
        Stream<Baza> stream = manager.stream();
        
        for (final Predicate<Baza> predicate : predicates) {
            stream = stream.filter(predicate);
        }
        
        if (!sorters.isEmpty()) {
            final Optional<Comparator<Baza>> comparator = sorters.stream()
                .reduce(Comparator::thenComparing);
            
            stream = stream.sorted(comparator.get());
        }
        
        return stream
            .skip(start)
            .limit(limit)
            .collect(JsonCollectors.toList(encoder));
    }
    
    /**
     * How to filter the results from the controller. This class is designed to
     * be deserialized automatically from JSON.
     */
    public final static class BazaFilter extends AbstractFilter<Baza> {
        
        public BazaFilter(
                String operator,
                String property,
                String value) {
            super(operator, property, value);
        }
        
        @Override
        public Predicate<Baza> toPredicate() {
            switch (property()) {
                case "lokalizacja" : {
                    final String v = value();
                    switch (operator()) {
                        case "eq"   : return Baza.LOKALIZACJA.equal(v);
                        case "ne"   : return Baza.LOKALIZACJA.notEqual(v);
                        case "lt"   : return Baza.LOKALIZACJA.lessThan(v);
                        case "le"   : return Baza.LOKALIZACJA.lessOrEqual(v);
                        case "gt"   : return Baza.LOKALIZACJA.greaterThan(v);
                        case "ge"   : return Baza.LOKALIZACJA.greaterOrEqual(v);
                        case "like" : return Baza.LOKALIZACJA.contains(v);
                        default : throw new IllegalArgumentException(
                            "'" + operator() + "' is not a valid operator for " +
                            "Baza.lokalizacja."
                        );
                    }
                }
                case "liczbaMiejsc" : {
                    final int v = Integer.parseInt(value());
                    switch (operator()) {
                        case "eq"   : return Baza.LICZBA_MIEJSC.equal(v);
                        case "ne"   : return Baza.LICZBA_MIEJSC.notEqual(v);
                        case "lt"   : return Baza.LICZBA_MIEJSC.lessThan(v);
                        case "le"   : return Baza.LICZBA_MIEJSC.lessOrEqual(v);
                        case "gt"   : return Baza.LICZBA_MIEJSC.greaterThan(v);
                        case "ge"   : return Baza.LICZBA_MIEJSC.greaterOrEqual(v);
                        case "like" : // Fallthrough
                        default : throw new IllegalArgumentException(
                            "'" + operator() + "' is not a valid operator for " +
                            "Baza.liczbaMiejsc."
                        );
                    }
                }
                default : throw new IllegalArgumentException(
                    "'" + property() + "' is not a valid Baza property."
                );
            }
        }
    }
    
    /**
     * How to sort the results from the controller. This class is designed to be
     * deserialized automatically from JSON.
     */
    public final static class BazaSort extends AbstractSort<Baza> {
        
        public BazaSort(String property, String direction) {
            super(property, direction);
        }
        
        @Override
        public Comparator<Baza> toComparator() {
            final Comparator<Baza> comparator;
            switch (property()) {
                case "lokalizacja"  : comparator = Baza.LOKALIZACJA.comparator();   break;
                case "liczbaMiejsc" : comparator = Baza.LICZBA_MIEJSC.comparator(); break;
                default : throw new IllegalArgumentException(
                    "'" + property() + "' is not a valid Baza property."
                );
            }
            
            switch (direction()) {
                case "ASC"  : return comparator;
                case "DESC" : return comparator.reversed();
                default : throw new IllegalArgumentException(
                    "'" + direction() + "' is not a valid sort direction. " +
                    "Use either 'ASC' or 'DESC', or leave out."
                );
            }
        }
    }
}