package com.company.carsharing.carsharing.carsharing.doladowanie_konta.generated;

import com.company.carsharing.carsharing.carsharing.doladowanie_konta.DoladowanieKonta;
import com.company.carsharing.carsharing.carsharing.doladowanie_konta.DoladowanieKontaManager;
import com.company.carsharing.carsharing.carsharing.doladowanie_konta.generated.GeneratedDoladowanieKonta.Identifier;
import com.speedment.common.annotation.GeneratedCode;
import com.speedment.common.json.Json;
import com.speedment.enterprise.plugins.json.JsonCollectors;
import com.speedment.enterprise.plugins.json.JsonComponent;
import com.speedment.enterprise.plugins.json.JsonEncoder;
import com.speedment.enterprise.plugins.spring.runtime.AbstractFilter;
import com.speedment.enterprise.plugins.spring.runtime.AbstractSort;
import com.speedment.enterprise.plugins.spring.runtime.ControllerUtil;
import com.speedment.runtime.field.Field;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.CrossOrigin;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RequestParam;

import java.sql.Date;
import java.util.Comparator;
import java.util.EnumSet;
import java.util.List;
import java.util.Optional;
import java.util.Set;
import java.util.function.Predicate;
import java.util.stream.Stream;
import javax.annotation.PostConstruct;

import static java.util.stream.Collectors.toList;

/**
 * The default REST controller logic for DoladowanieKonta entities.
 * <p>
 * This file has been automatically generated by Speedment. Any changes made to
 * it will be overwritten.
 * 
 * @author Speedment
 */
@GeneratedCode("Speedment")
@CrossOrigin(origins = "*", maxAge = 3600)
public abstract class GeneratedDoladowanieKontaController {
    
    protected @Autowired JsonComponent jsonComponent;
    protected @Autowired DoladowanieKontaManager manager;
    protected JsonEncoder<DoladowanieKonta> encoder;
    
    @PostConstruct
    void createDoladowanieKontaEncoder() {
        encoder = jsonComponent.<DoladowanieKonta>emptyEncoder()
            .put("data", DoladowanieKonta.DATA)
            .put("kwota", DoladowanieKonta.KWOTA)
            .put("sposobPlatnosci", DoladowanieKonta.SPOSOB_PLATNOSCI)
            .put("uzytkownikMail", DoladowanieKonta.UZYTKOWNIK_MAIL)
            .build();
    }
    
    @GetMapping(path = "/doladowanie_konta", produces = "application/json")
    public String get(
            @RequestParam(name = "filter", defaultValue = "[]") String filters,
            @RequestParam(name = "sort", defaultValue = "[]") String sorters,
            @RequestParam(value = "start", defaultValue = "0") long start,
            @RequestParam(value = "limit", defaultValue = "25") long limit) {
        
        return getHelper(
            ControllerUtil.parseFilters(filters, DoladowanieKontaFilter::new).collect(toList()),
            ControllerUtil.parseSorts(sorters, DoladowanieKontaSort::new).collect(toList()),
            start,
            limit
        );
    }
    
    protected final Set<Identifier> parseColumns(String jsonColumnList) {
        try {
            @SuppressWarnings("unchecked")
            final List<String> parsed = (List<String>) Json.fromJson(jsonColumnList);
            final Set<GeneratedDoladowanieKonta.Identifier> result = EnumSet.noneOf(GeneratedDoladowanieKonta.Identifier.class);
            parsed.stream().map(this::parseColumn).forEach(result::add);
            return result;
        } catch (final ClassCastException ex) {
            throw new IllegalArgumentException("Error in parsed JSON.");
        }
    }
    
    protected final Identifier parseColumn(String jsonColumn) {
        switch (jsonColumn) {
            case "data":            return GeneratedDoladowanieKonta.Identifier.DATA;
            case "kwota":           return GeneratedDoladowanieKonta.Identifier.KWOTA;
            case "sposobPlatnosci": return GeneratedDoladowanieKonta.Identifier.SPOSOB_PLATNOSCI;
            case "uzytkownikMail":  return GeneratedDoladowanieKonta.Identifier.UZYTKOWNIK_MAIL;
            default: throw new IllegalArgumentException(
                "Unknown column '" + jsonColumn + "'."
            );
        }
    }
    
    protected final Field<DoladowanieKonta> fieldOf(Identifier columnId) {
        switch (columnId) {
            case DATA:             return DoladowanieKonta.DATA;
            case KWOTA:            return DoladowanieKonta.KWOTA;
            case SPOSOB_PLATNOSCI: return DoladowanieKonta.SPOSOB_PLATNOSCI;
            case UZYTKOWNIK_MAIL:  return DoladowanieKonta.UZYTKOWNIK_MAIL;
            default: throw new IllegalArgumentException(
                "Unknown column '" + columnId + "'."
            );
        }
    }
    
    protected String getHelper(
            List<Predicate<DoladowanieKonta>> predicates,
            List<Comparator<DoladowanieKonta>> sorters,
            long start,
            long limit) {
        Stream<DoladowanieKonta> stream = manager.stream();
        
        for (final Predicate<DoladowanieKonta> predicate : predicates) {
            stream = stream.filter(predicate);
        }
        
        if (!sorters.isEmpty()) {
            final Optional<Comparator<DoladowanieKonta>> comparator = sorters.stream()
                .reduce(Comparator::thenComparing);
            
            stream = stream.sorted(comparator.get());
        }
        
        return stream
            .skip(start)
            .limit(limit)
            .collect(JsonCollectors.toList(encoder));
    }
    
    /**
     * How to filter the results from the controller. This class is designed to
     * be deserialized automatically from JSON.
     */
    public final static class DoladowanieKontaFilter extends AbstractFilter<DoladowanieKonta> {
        
        public DoladowanieKontaFilter(
                String operator,
                String property,
                String value) {
            super(operator, property, value);
        }
        
        @Override
        public Predicate<DoladowanieKonta> toPredicate() {
            switch (property()) {
                case "data" : {
                    final Date v = Date.valueOf(value());
                    switch (operator()) {
                        case "eq"   : return DoladowanieKonta.DATA.equal(v);
                        case "ne"   : return DoladowanieKonta.DATA.notEqual(v);
                        case "lt"   : return DoladowanieKonta.DATA.lessThan(v);
                        case "le"   : return DoladowanieKonta.DATA.lessOrEqual(v);
                        case "gt"   : return DoladowanieKonta.DATA.greaterThan(v);
                        case "ge"   : return DoladowanieKonta.DATA.greaterOrEqual(v);
                        case "like" : // Fallthrough
                        default : throw new IllegalArgumentException(
                            "'" + operator() + "' is not a valid operator for " +
                            "DoladowanieKonta.data."
                        );
                    }
                }
                case "kwota" : {
                    final double v = Double.parseDouble(value());
                    switch (operator()) {
                        case "eq"   : return DoladowanieKonta.KWOTA.equal(v);
                        case "ne"   : return DoladowanieKonta.KWOTA.notEqual(v);
                        case "lt"   : return DoladowanieKonta.KWOTA.lessThan(v);
                        case "le"   : return DoladowanieKonta.KWOTA.lessOrEqual(v);
                        case "gt"   : return DoladowanieKonta.KWOTA.greaterThan(v);
                        case "ge"   : return DoladowanieKonta.KWOTA.greaterOrEqual(v);
                        case "like" : // Fallthrough
                        default : throw new IllegalArgumentException(
                            "'" + operator() + "' is not a valid operator for " +
                            "DoladowanieKonta.kwota."
                        );
                    }
                }
                case "sposobPlatnosci" : {
                    final String v = value();
                    switch (operator()) {
                        case "eq"   : return DoladowanieKonta.SPOSOB_PLATNOSCI.equal(v);
                        case "ne"   : return DoladowanieKonta.SPOSOB_PLATNOSCI.notEqual(v);
                        case "lt"   : return DoladowanieKonta.SPOSOB_PLATNOSCI.lessThan(v);
                        case "le"   : return DoladowanieKonta.SPOSOB_PLATNOSCI.lessOrEqual(v);
                        case "gt"   : return DoladowanieKonta.SPOSOB_PLATNOSCI.greaterThan(v);
                        case "ge"   : return DoladowanieKonta.SPOSOB_PLATNOSCI.greaterOrEqual(v);
                        case "like" : return DoladowanieKonta.SPOSOB_PLATNOSCI.contains(v);
                        default : throw new IllegalArgumentException(
                            "'" + operator() + "' is not a valid operator for " +
                            "DoladowanieKonta.sposobPlatnosci."
                        );
                    }
                }
                case "uzytkownikMail" : {
                    final String v = value();
                    switch (operator()) {
                        case "eq"   : return DoladowanieKonta.UZYTKOWNIK_MAIL.equal(v);
                        case "ne"   : return DoladowanieKonta.UZYTKOWNIK_MAIL.notEqual(v);
                        case "lt"   : return DoladowanieKonta.UZYTKOWNIK_MAIL.lessThan(v);
                        case "le"   : return DoladowanieKonta.UZYTKOWNIK_MAIL.lessOrEqual(v);
                        case "gt"   : return DoladowanieKonta.UZYTKOWNIK_MAIL.greaterThan(v);
                        case "ge"   : return DoladowanieKonta.UZYTKOWNIK_MAIL.greaterOrEqual(v);
                        case "like" : return DoladowanieKonta.UZYTKOWNIK_MAIL.contains(v);
                        default : throw new IllegalArgumentException(
                            "'" + operator() + "' is not a valid operator for " +
                            "DoladowanieKonta.uzytkownikMail."
                        );
                    }
                }
                default : throw new IllegalArgumentException(
                    "'" + property() + "' is not a valid DoladowanieKonta property."
                );
            }
        }
    }
    
    /**
     * How to sort the results from the controller. This class is designed to be
     * deserialized automatically from JSON.
     */
    public final static class DoladowanieKontaSort extends AbstractSort<DoladowanieKonta> {
        
        public DoladowanieKontaSort(String property, String direction) {
            super(property, direction);
        }
        
        @Override
        public Comparator<DoladowanieKonta> toComparator() {
            final Comparator<DoladowanieKonta> comparator;
            switch (property()) {
                case "data"            : comparator = DoladowanieKonta.DATA.comparator();             break;
                case "kwota"           : comparator = DoladowanieKonta.KWOTA.comparator();            break;
                case "sposobPlatnosci" : comparator = DoladowanieKonta.SPOSOB_PLATNOSCI.comparator(); break;
                case "uzytkownikMail"  : comparator = DoladowanieKonta.UZYTKOWNIK_MAIL.comparator();  break;
                default : throw new IllegalArgumentException(
                    "'" + property() + "' is not a valid DoladowanieKonta property."
                );
            }
            
            switch (direction()) {
                case "ASC"  : return comparator;
                case "DESC" : return comparator.reversed();
                default : throw new IllegalArgumentException(
                    "'" + direction() + "' is not a valid sort direction. " +
                    "Use either 'ASC' or 'DESC', or leave out."
                );
            }
        }
    }
}